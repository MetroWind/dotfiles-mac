#!/usr/bin/env bash

source ../lib/report.sh

brew cask install squirrel

# Copy files
mkdir -pv ${HOME}/Library/Rime
# Since version 0.15(?), linking config files stopped working...
cp files/*.yaml ${HOME}/Library/Rime/
